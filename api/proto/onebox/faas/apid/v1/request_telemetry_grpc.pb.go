// request_telemetry — production debugger per-request data plane
// (ADR-127 PR-B). gatewayd-internal dials apid on /run/faas/apid.sock
// to forward collapsed (app, deployment, route, method, status,
// minute) buckets. apid is the sole writer to request_telemetry
// (CLAUDE.md ownership); gatewayd-internal never opens a direct
// Postgres connection for this path.
//
// Wire discipline mirrors the sibling apid/v1/app_errors.proto
// service: checked-in *.pb.go generated with paths=source_relative,
// errors enveloped through pkg/grpcerr, auth is the unix-socket DAC
// model (mode 0660 group `faas`, ADR-015) on /run/faas/apid.sock.
//
// Streaming: IncrementRequestTelemetry is a server-streaming RPC
// so a bursty gateway can ship a batched window of events without
// paying per-record RPC overhead. The server reads until the
// client half-closes the stream; each event is committed inside
// its own transaction (no dedupe-merge here — the publisher
// already collapsed burst traffic into one row per bucket, so
// the apid receiver's only atomicity requirement is per-row
// INSERT). The PR-B rate limiter returns per-row
// {code: RATE_LIMITED, retry_after_ms} outcome so a sustained-
// overflow customer gets back-pressured without aborting the rest
// of the batch.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// source: onebox/faas/apid/v1/request_telemetry.proto

package apidpb

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RequestTelemetry_IncrementRequestTelemetry_FullMethodName = "/onebox.faas.apid.v1.RequestTelemetry/IncrementRequestTelemetry"
)

// RequestTelemetryClient is the client API for RequestTelemetry service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// RequestTelemetry is the gRPC service apid exposes on
// /run/faas/apid.sock for the gatewayd-internal production
// debugger publisher (ADR-127). Single RPC accepts a stream of
// collapsed telemetry rows; the server returns per-record outcomes
// so the gateway can update its in-process counters (shipped /
// rate_limited / db_error).
type RequestTelemetryClient interface {
	// IncrementRequestTelemetry streams collapsed telemetry rows
	// from the gateway edge to apid's writer path. The server
	// commits each record inside its own transaction (per-record
	// commit; load-bearing for the rate-limit-then-continues
	// shape — a rate-limited row MUST NOT abort the batch).
	//
	// The response stream mirrors the request stream 1:1: every
	// request gets exactly one response carrying its outcome. The
	// server half-closes the response stream when the client
	// half-closes the request stream (or on the first terminal
	// error). A rate-limit hit on one record returns RATE_LIMITED
	// for that single response — the stream continues.
	IncrementRequestTelemetry(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse], error)
}

type requestTelemetryClient struct {
	cc grpc.ClientConnInterface
}

func NewRequestTelemetryClient(cc grpc.ClientConnInterface) RequestTelemetryClient {
	return &requestTelemetryClient{cc}
}

func (c *requestTelemetryClient) IncrementRequestTelemetry(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &RequestTelemetry_ServiceDesc.Streams[0], RequestTelemetry_IncrementRequestTelemetry_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RequestTelemetry_IncrementRequestTelemetryClient = grpc.BidiStreamingClient[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]

// RequestTelemetryServer is the server API for RequestTelemetry service.
// All implementations must embed UnimplementedRequestTelemetryServer
// for forward compatibility.
//
// RequestTelemetry is the gRPC service apid exposes on
// /run/faas/apid.sock for the gatewayd-internal production
// debugger publisher (ADR-127). Single RPC accepts a stream of
// collapsed telemetry rows; the server returns per-record outcomes
// so the gateway can update its in-process counters (shipped /
// rate_limited / db_error).
type RequestTelemetryServer interface {
	// IncrementRequestTelemetry streams collapsed telemetry rows
	// from the gateway edge to apid's writer path. The server
	// commits each record inside its own transaction (per-record
	// commit; load-bearing for the rate-limit-then-continues
	// shape — a rate-limited row MUST NOT abort the batch).
	//
	// The response stream mirrors the request stream 1:1: every
	// request gets exactly one response carrying its outcome. The
	// server half-closes the response stream when the client
	// half-closes the request stream (or on the first terminal
	// error). A rate-limit hit on one record returns RATE_LIMITED
	// for that single response — the stream continues.
	IncrementRequestTelemetry(grpc.BidiStreamingServer[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]) error
	mustEmbedUnimplementedRequestTelemetryServer()
}

// UnimplementedRequestTelemetryServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRequestTelemetryServer struct{}

func (UnimplementedRequestTelemetryServer) IncrementRequestTelemetry(grpc.BidiStreamingServer[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]) error {
	return status.Error(codes.Unimplemented, "method IncrementRequestTelemetry not implemented")
}
func (UnimplementedRequestTelemetryServer) mustEmbedUnimplementedRequestTelemetryServer() {}
func (UnimplementedRequestTelemetryServer) testEmbeddedByValue()                          {}

// UnsafeRequestTelemetryServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RequestTelemetryServer will
// result in compilation errors.
type UnsafeRequestTelemetryServer interface {
	mustEmbedUnimplementedRequestTelemetryServer()
}

func RegisterRequestTelemetryServer(s grpc.ServiceRegistrar, srv RequestTelemetryServer) {
	// If the following call panics, it indicates UnimplementedRequestTelemetryServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RequestTelemetry_ServiceDesc, srv)
}

func _RequestTelemetry_IncrementRequestTelemetry_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(RequestTelemetryServer).IncrementRequestTelemetry(&grpc.GenericServerStream[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RequestTelemetry_IncrementRequestTelemetryServer = grpc.BidiStreamingServer[IncrementRequestTelemetryRequest, IncrementRequestTelemetryResponse]

// RequestTelemetry_ServiceDesc is the grpc.ServiceDesc for RequestTelemetry service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RequestTelemetry_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "onebox.faas.apid.v1.RequestTelemetry",
	HandlerType: (*RequestTelemetryServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "IncrementRequestTelemetry",
			Handler:       _RequestTelemetry_IncrementRequestTelemetry_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "onebox/faas/apid/v1/request_telemetry.proto",
}
