# Sidecars

Sidecars are now documented as [application companions](./companions.md).
The existing `sidecars` API field and `extensions` manifest key remain
accepted for backwards compatibility, but new integrations should use
`companions`.
