# Build artifacts
/bin/
*.test
*.out
coverage.*

# Local config & secrets — never commit
/etc/faas/
*.local.toml
secrets/

# Firecracker / VM working state
/srv/fc/
*.ext4
*.snap
vmlinux-*

# Editor / OS
.DS_Store
.idea/
.vscode/

# Stray binary at repo root from `make metal-lima` / local guest-init
# build artifact (Mach-O arm64 ~5MB). Distinct from guest/init/, which
# is the directory holding the actual guest-init source.
/init
