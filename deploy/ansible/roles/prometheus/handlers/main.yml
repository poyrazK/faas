- name: daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart prometheus
  ansible.builtin.service:
    name: prometheus
    state: restarted

- name: reload prometheus
  ansible.builtin.service:
    name: prometheus
    state: reloaded
