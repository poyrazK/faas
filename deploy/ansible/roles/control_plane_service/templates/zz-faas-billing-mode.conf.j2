[Service]
# Public-beta billing activation switch. The release unit defaults to live;
# this role intentionally overrides it until the launch-date transition.
Environment=FAAS_BILLING_MODE={{ faas_billing_mode }}
